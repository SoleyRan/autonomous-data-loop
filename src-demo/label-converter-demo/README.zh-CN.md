# 统一标签转换 Demo

[English](README.md)

这个 demo 将简化的统一标签 JSON 转换为类似 KITTI 的文本行。

它用于展示阶段二的导出适配器思路，不是完整的 KITTI 或 nuScenes 转换器。

## 运行

```powershell
python canonical_to_kitti.py ..\..\examples\label-format\canonical-label.example.json
```

