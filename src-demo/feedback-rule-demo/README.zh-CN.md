# 回灌探针规则生成 Demo

[English](README.md)

这个 demo 将回灌 case 转化为场景数据需求和探针规则建议。

它展示阶段六的核心思想：失败 case 不应该停留在报告里，而应该转化为定向数据采集工作。

## 运行

```powershell
python build_probe_rule.py `
  ..\..\examples\feedback\feedback-case.example.json
```

